build:
  docker:
    web: Dockerfile
  config:
    WORKDIR: app
    API_URL: https://coconuts-spa-7cedf937f496.herokuapp.com
    # 例) API_URL: https://my-app.herokuapp.com
run:
  web: yarn run preview