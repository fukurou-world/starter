build:
  docker:
    web: Dockerfile
  config:
    WORKDIR: app
    API_URL: https://coconuts-poker-87d283940a6e.herokuapp.com/
    # 例) API_URL: https://my-app.herokuapp.com
run:
  web: yarn run start